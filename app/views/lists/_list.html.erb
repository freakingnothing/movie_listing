<li class="list-group-item p-0">
  <%= link_to(list_path(list), class: "p-3 d-flex justify-content-between align-items-center list-group-item-action") do %>
    <span><%= list.title %></span>
    <span class="badge badge-info badge-pill"><%= list.movies.size %></span>
  <% end %>
</li>
